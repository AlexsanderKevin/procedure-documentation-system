<div class="item_content_container ">
    <div class="item_content_header">                
        <h2 id="{{id}}">{{title}}</h2>
        {{>main/burger class = 'page_content_burger content_burger all_button'}}
    </div>
    <div class="item_content_text">
        {{#each category}}
        {{#if this.dataValues.approved}}
        <div class="inner_div">
        <div class="single_container showed_content">
            <div class="inner_div">
                <div class="content_header">
                    <h3>
                        <span class="{{../target}}_title_text">
                            {{>id_span}}
                            {{this.dataValues.title}}
                        </span>
                        <!-- edit subsection title form -->
                        {{>edition/forms/update/title
                            target=../target
                            id=this.dataValues.id
                            collumn='title'
                            input_value=this.dataValues.title
                            redirect=../redirect
                            form_class=../form_class
                            }}

                        {{>edition/button/edit class=../update_button_class}}
                    </h3>
                    {{>main/burger class='single_content_burger content_burger all_button'}}
                </div>
                <div class="content_text">
                    <p class="{{../target}}_description_text">
                        {{{this.dataValues.content}}}
                    </p>

                    {{>edition/forms/update/description
                        target=../target
                        id=this.dataValues.id
                        form_class=../content_form_class
                        redirect=../redirect
                        collumn='content'
                        input_value=this.dataValues.content
                        input_style='height: 200px; min-width: 100%; width: 900px'
                    }}

                    {{>edition/button/edit class=../update_content_button_class}}
                </div>
            </div>
        {{>@partial-block}}
        </div>
        </div>
        {{/if}}
        {{/each}}
    </div>
</div>