<div class="checkbox_container">
    <input type="checkbox" class="{{input_class}}" name="{{name}}" id="{{name}}_{{id}}" {{checked}}>
    <label class="all_button" for="{{name}}_{{id}}" id="{{name}}_label">{{label}}</label>
</div>