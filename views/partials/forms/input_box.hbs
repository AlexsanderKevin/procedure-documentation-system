<div class="input_box {{class}}">
    <input id="{{name}}" class="text_input" type="{{type}}" name="{{name}}" value="{{value}}" required></input>
    <label for="{{name}}" class="edit_label">{{label}}</label>
</div>