<section id="add_user_form" class="register form_section edition_form show">
    <header class="form_header">
        <h1>Novo Usuário</h1>
    </header>
    <form>
        {{>template_form_input_box name="name" type="text" text="*Nome Completo"}}
        {{>template_form_input_box name="username" type="text" text="*Nome de Usuário"}}

        <div class="password_container">
            {{>template_form_input_box name="password" type="password" text="*Senha"}}
            {{>template_form_input_box name="confirm_password" type="password" text="*Confirmar Senha"}}
        </div>

        {{>template_form_datalist_input name="user_department" text="*Departamentos"}}
        <datalist name="user_department" id="user_department">
            {{#departments}}
            <option value="{{this.dataValues.name}}">
            {{/departments}}
        </datalist>
        {{>template_form_button_container}}
    </form>
</section>